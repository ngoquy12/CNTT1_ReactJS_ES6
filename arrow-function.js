// Khai báo hàm thông thường
// function getName(name) {
//   return `Xin chào ${name}`;
// }

// Khai báo hàm arrow
const getName = (name) => {
  return `Xin chào ${name}`;
};

// Cách viết tắt của arrow function
const sum = (a, b) => a + b;

console.log(getName("Nguyễn Văn A"));
